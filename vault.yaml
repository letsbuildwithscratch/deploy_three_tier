# vault.yml (store with ansible-vault)
postgres_db: appdb
postgres_user: appuser
postgres_password: SuperSecretDBPassw0rd!
secret_key: a_long_random_secret_key_here_change_it
app_api_url: "http://localhost:8000"