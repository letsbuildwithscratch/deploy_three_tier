# .env.example - commit this to show required keys (no real secrets here)
API_URL=http://localhost:8000
DATABASE_URL=postgresql://<replace_user>:<replace_password>@db:5432/<replace_db>
SECRET_KEY=replace-with-a-random-secret
POSTGRES_DB=appdb
POSTGRES_USER=appuser
POSTGRES_PASSWORD=replace-with-a-secure-password